# Быстрый старт: Готовые конфигурации для Zapret YouTube

## ⚠️ ВАЖНО ДЛЯ ВАШЕГО РОУТЕРА TP-Link EC220-G5 v2.0

Ваш роутер имеет **слабый процессор** (не более 300 МГц). Избегайте сложных стратегий с множественными фейками и высокими repeats!

---

## ВАРИАНТ 1️⃣ - САМЫЙ БЫСТРЫЙ (Попробуйте первым!)

Место: `/opt/zapret/config`

```bash
MODE=nfqws
MODE_HTTP=0
MODE_HTTPS=1
MODE_QUIC=1
MODE_FILTER=hostlist

NFQWS_OPT_DESYNC="--dpi-desync=split2"
NFQWS_OPT_DESYNC_QUIC="--dpi-desync=fake --dpi-desync-repeats=6"
```

**Команды для быстрого применения:**

```bash
# 1. Отредактируйте конфиг
ssh admin@192.168.0.1
# пароль: admin (или admin/admin)

# 2. Отредактируйте файл
vi /opt/zapret/config

# Найдите строки MODE, NFQWS_OPT_DESYNC, NFQWS_OPT_DESYNC_QUIC
# Замените их на конфиг выше
# Нажмите ESC, затем :wq (для сохранения)

# 3. Перезапустите Zapret
/etc/init.d/zapret restart

# 4. Проверьте статус
/etc/init.d/zapret status

# 5. Если есть ошибки, посмотрите логи
logread | grep zapret
```

---

## ВАРИАНТ 2️⃣ - ЕСЛИ ВАРИАНТ 1 НЕ РАБОТАЕТ (Средний уровень)

```bash
MODE=nfqws
MODE_HTTP=0
MODE_HTTPS=1
MODE_QUIC=1
MODE_FILTER=hostlist

NFQWS_OPT_DESYNC="--dpi-desync=multidisorder --dpi-desync-split-pos=1,midsld --dpi-desync-repeats=2 --dpi-desync-fooling=badseq --dpi-desync-autottl=2:2-12"
NFQWS_OPT_DESYNC_QUIC="--dpi-desync=fake --dpi-desync-repeats=4 --dpi-desync-fake-quic=/opt/zapret/files/fake/quic_initial_www_google_com.bin"
```

---

## ВАРИАНТ 3️⃣ - МАКСИМАЛЬНАЯ ДОСТУПНОСТЬ (Если нужна гарантия)

```bash
MODE=nfqws
MODE_HTTP=0
MODE_HTTPS=1
MODE_QUIC=1
MODE_FILTER=hostlist

NFQWS_OPT_DESYNC="--dpi-desync=fake,multidisorder --dpi-desync-split-pos=1,midsld --dpi-desync-repeats=2 --dpi-desync-fooling=badseq --dpi-desync-split-seqovl=681 --dpi-desync-split-seqovl-pattern=/opt/zapret/files/fake/tls_clienthello_www_google_com.bin --dpi-desync-fake-tls-mod=rnd,dupsid,sni=fonts.google.com"
NFQWS_OPT_DESYNC_QUIC="--dpi-desync=fake --dpi-desync-repeats=4 --dpi-desync-fake-quic=/opt/zapret/files/fake/quic_initial_www_google_com.bin"
```

---

## 📝 Не забудьте обновить список хостов!

Отредактируйте `/opt/zapret/ipset/zapret-hosts-user.txt`

```bash
# SSH в роутер
ssh admin@192.168.0.1

# Отредактируйте файл
vi /opt/zapret/ipset/zapret-hosts-user.txt

# Добавьте эти домены (если их еще нет):
googlevideo.com
youtubei.googleapis.com
i.ytimg.com
yt3.ggpht.com
youtube.com
youtubeembeddedplayer.googleapis.com
yt4.ggpht.com
```

---

## 🔄 Как переключаться между вариантами

1. SSH в роутер: `ssh admin@192.168.0.1`
2. Отредактируйте `/opt/zapret/config`
3. Замените значения NFQWS_OPT_DESYNC и NFQWS_OPT_DESYNC_QUIC
4. Выполните: `/etc/init.d/zapret restart`
5. **Ждите 20 секунд** перед тестированием
6. Откройте YouTube в браузере
7. Если не работает - попробуйте следующий вариант

---

## ✅ Как проверить, что все работает

```bash
# В браузере откройте консоль разработчика (F12)
# Перейдите на youtube.com
# Откройте вкладку "Сеть" (Network)
# Посмотрите на строки запросов к googlevideo.com

# Должны быть статусы 206 (Partial Content) или 200 (OK)
# НЕ должны быть 502, 503, 504 (это значит DPI заблокировал)
```

---

## 🚀 Супер-быстрый способ (через веб-интерфейс роутера)

1. Откройте `192.168.0.1` в браузере
2. Логин: `admin`, Пароль: `admin`
3. Найдите раздел "Zapret" или "DPI Bypass"
4. Нажмите на стратегию YouTube
5. Перезагрузитесь
6. Готово!

---

## ❓ Если ничего не работает

1. **Проверьте, что Zapret установлен:**
   ```bash
   ls -la /opt/zapret/
   ```

2. **Проверьте процесс:**
   ```bash
   ps | grep nfqws
   ```

3. **Если процесса нет - запустите вручную:**
   ```bash
   /etc/init.d/zapret start
   ```

4. **Посмотрите ошибки:**
   ```bash
   /etc/init.d/zapret stop
   /usr/bin/nfqws --help
   ```

5. **Перезагрузитесь целиком:**
   ```bash
   reboot
   ```

---

## 💡 Про параметры (если хотите понять)

- `--dpi-desync=split2` - разбивает TLS пакет на 2 части
- `--dpi-desync=multidisorder` - разбивает в нескольких местах
- `--dpi-desync=fake,multidisorder` - отправляет поддельный пакет И разбивает
- `--dpi-desync-repeats=N` - сколько раз повторять трюк
- `--dpi-desync-fooling=badseq` - отправляет пакеты с неправильными номерами
- `--dpi-desync-autottl=2:2-12` - автоматически подбирает TTL в диапазоне 2-12

---

## ⏱️ Время между сменой стратегии

После каждого изменения ждите **минимум 30 секунд** перед проверкой:

```
Редактируем конфиг (2 сек)
    ↓
Перезапускаем Zapret (5 сек)
    ↓
Ждем (20 сек) ← ВАЖНО!
    ↓
Открываем YouTube (5 сек)
    ↓
Загружается видео (5-10 сек)
```

Если поспешить - YouTube может показать кэшированное ошибку и вы подумаете, что стратегия не работает!

---

## 📌 Порядок тестирования

1. **День 1:** Вариант 1 (самый простой)
2. **День 2:** Вариант 2 (если вариант 1 не сработал)
3. **День 3:** Вариант 3 (максимальный)
4. **День 4:** Ищите обновления Zapret, может новая версия лучше подходит

